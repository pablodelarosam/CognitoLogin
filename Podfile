# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'MigrationUser' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for MigrationUser
	pod 'AWSCore', '~> 2.9.0'
        pod 'AWSMobileClient', '~> 2.9.0'      # Required dependency
        pod 'AWSAuthUI', '~> 2.9.0'            # Optional dependency required to use drop

        pod 'AWSUserPoolsSignIn', '~> 2.9.0'   # Optional dependency required to use dro

  target 'MigrationUserTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
